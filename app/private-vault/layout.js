export const metadata = {
  title: "Private Health Vault | Agro Valley Co.",
  description: "Private health records area.",
  robots: { index: false, follow: false, googleBot: { index: false, follow: false } },
};

export default function PrivateVaultLayout({ children }) {
  return children;
}
